# Lina 数字人 · 三大功能实现说明（汇报用）

> 给领导汇报用。每节包含：① 怎么做的 ② 代码位置 ③ 完整 prompt。
> 总原则：所有"怎么回这一轮"的决策都先经过 **controller 决策层**（规则层秒判 →
> 判不准 fan-out 到 gpt-5-mini 微顾问 → 产出结构化 Plan），再据 Plan 检索 + 推理。

---

## 一、长回复切分（解决"回复过长"）

### 怎么做的
1. **controller 给"切分预算"**：每轮 Plan 里有两个字段 `allow_segment`（本轮准不准拆）
   和 `max_segments`（最多几段）。问候/告别/短反应/能力边界/续说/主动发言 → 不准拆；
   兴奋点/安抚/关系回访/自我反思等可展开场景 → 准拆。
2. **主模型在预算内执行**：主系统 prompt 里有"分段说话机制"，告诉主模型——内容有多个
   独立意思时，**第一段直接发**，其余写成隐藏标记 `[segments: 要点A || 要点B]` 挂在末尾。
3. **系统解析**：`parse_segments_tag()` 把第一段留给用户显示，`[segments:]` 标记**剥掉**
   （用户看不到、TTS 不会念），其余要点存进 `conversation.pending_segments`。
4. **逐段续说**：用户沉默时，前端短计时器（~3 秒）调 `/api/continue` →
   `continue_segment()` 弹出下一个要点，让主模型自然展开成一小段补发。
   **用户一插话，pending_segments 被新一轮覆盖/清空，未说完的段作废。**

### 代码位置
| 内容 | 文件:位置 |
|---|---|
| 切分预算字段 `allow_segment`/`max_segments` | `app/controller/schema.py`（LinaPromptPlan） |
| 规则层判切不切 | `app/controller/rule_router.py`（各分支 `allow_segment=`） |
| LLM 微顾问判切不切 | `app/controller/experts.py`（`allow_segment` advisor） |
| 把预算写进本轮约束 | `app/controller/composer.py:_build_constraint_block` |
| 切分标记正则 + 解析 | `app/character.py`：`SEGMENTS_TAG_RE`、`parse_segments_tag()`（约 50-76 行） |
| 主 prompt 的分段机制说明 | `app/character.py`：`SEGMENT_PROTOCOL_SPEC`（约 270 行） |
| 解析 + 尊重预算（截断/丢弃） | `app/character.py:chat()`（约 796-806 行） |
| 续说方法 | `app/character.py:continue_segment()` |
| 续说端点 | `app/web.py:/api/continue` |
| 待续段字段 | `app/conversation.py`：`pending_segments` |
| 上限常量 | `app/character.py`：`MAX_PENDING_SEGMENTS = 3` |

### Prompt —— 主模型的"分段说话机制"（`SEGMENT_PROTOCOL_SPEC`，常驻系统 prompt）
```
# 分段说话机制（机制要求，重要）

你说话像发微信，不是写信。一条消息只说一个意思、一两口气就发出去，
剩下想说的分多条接着发。绝不要把好几个意思、好几句追问堆进一大段。

判断标准——只要满足任意一条，就必须分段：
- 这一轮你想表达的内容包含超过一个独立的意思（比如：先回应对方 + 又抛一个自己的联想；或连着问两个不同的问题）；
- 或者你发现自己这一段快要超过本轮的字数上限了。

分段怎么做：
- 这次的回复正文，只说第一个意思那一小段（照常 1-2 短行，照常带 [mood:…] 开头）。
- 在回复的最末尾另起一行，用标记列出剩下每一小段的要点（不是完整句子，是几个字的提纲）：
  [segments: 第二段的要点 || 第三段的要点]
- 这一行只给系统读、会被删掉，用户看不到。系统会在用户没接话时，让你把这些要点一段一段自然说完，中间用户随时插话就作废。

约束：
- 真的只有一个意思、一两口气能说完的短回复（问候、短反应、简单接话），就直接说完，不要硬凑分段。
- 最多列 3 个要点。
- 第一小段要能独立成立（用户不等后续也不突兀）。
- 要点之间用 || 分隔。只有一个意思时不要输出这一行。

反例（错误，把三个意思堆成一大段）：
  「哦是有人主动改的……那简化是怎么简化的？把笔画减少了？还是有些部分省掉了？那卷遗物上面混着方块字，我现在有点好奇那些是简体还是繁体了。」
正确做法（第一段只说一个意思，其余 park 起来）：
  正文：「哦——是有人主动去改的啊。」
  [segments: 追问简化是怎么简化的、是减笔画还是省部分 || 联想到自己那卷混着方块字的遗物，好奇那些是简体还是繁体]
```

### Prompt —— 续说指令（`CONTINUE_INSTRUCTION`，`{point}` 填入要点）
```
（系统提示，不要在回复里提到这段话，也不要说「接下来」「继续」之类的元叙述。）
你上一条话还没说完，现在自然地接着往下说一小段，围绕这个要点：
{point}
像发微信补一条那样，直接接着上一段的语气往下说，不要重新打招呼、不要重复刚说过的、不要总结。短，1-2 行就好。照常用 [mood:…] 开头。
```

### Prompt —— 切分预算（composer 写进本轮约束块，二选一）
```
不准拆时：- 本轮不要分段（不输出 [segments:…]），一口气把话说完即可。
准拆时：  - 本轮如果有多个意思可以分段说：第一段先发，其余写进 [segments:…]，最多 N 段；只有一个意思就别硬凑。
```

---

## 二、主动发言（用户沉默时主动开口）

### 怎么做的
**两阶段 + 四级递进 + 指数退避 + 话头去重。**

**节奏（前端计时器，参数后端下发、可环境变量调）：**
- 先续说阶段：还有没说完的段 → 每 ~3 秒补一段。
- 再主动找话阶段：段说完后才开始。**间隔 = 指数退避 + 随机抖动**：
  ```
  下一次等待 = engage_base_ms × engage_multiplier^已主动次数 × (1 ± jitter)
  ```
  默认值：`engage_base_ms=30000`（首次 30 秒）、`engage_multiplier=1.4`、`jitter=±30%`、`max_nudges=4`。
  → 大致：第1次 ~30秒、第2次 ~42秒、第3次 ~59秒、第4次告别。**越没人理等得越久，最后识趣告别。**
  （前端 UI 那个"首次 N 分钟"会覆盖 engage_base_ms。）

**四级话题递进（用户每次不回应升一级，到第4级告别后停）：**
1. 接**最近**聊到的话题
2. 翻**更早**聊过的话题
3. 转为说**莉娜自己**的经历
4. **告别并结束**

**怎么规避每次讲相同话题（两道）：**
1. **话头由 gpt-5-mini 挑**：不是写死的，而是每次从最近历史里挑"最该重提、且与用户相关"的话头（`pick_proactive_topic`），并按当前 stage 换策略（recent/earlier/self）。
2. **去重**：把本会话**已经抛过的话头**（存在消息 meta 的 `topic_hook`）作为 `avoid_hooks` 传给挑选器，prompt 明确"已抛过这些，这次必须换一个"。
- 另：**告别由后端按权威历史计数判定**（数实际主动了几次），前端计数漂移也不会漏告别、不会主动到第6次还不停。

### 代码位置
| 内容 | 文件:位置 |
|---|---|
| 节奏参数（magic numbers） | `app/config.py:resolve_proactive_pacing()`（约 100-107 行） |
| 前端退避计算 + 轮询 | `app/templates/chat.html`：`nextDelayMs()`、`withJitter()`、`PROACTIVE_POLL_MS`（约 1453-1468 行）；`maybeProactive/runContinue/runProactive` |
| 四级 stage 推导（按已主动次数） | `app/character.py:_run_proactive()`（`prior_proactive` → stage） |
| 话头挑选（含 stage、去重） | `app/controller/controller.py:pick_proactive_topic()` |
| 告别权威判定 + 已告别即停 | `app/character.py:_run_proactive()`（`is_farewell` 覆盖）、`app/web.py:/api/proactive`（`already_farewelled`） |
| 主动/告别端点 | `app/web.py:/api/proactive` |

### Prompt —— 话头挑选（`proactive_topic.txt`）
```
你在帮数字人「西比莉娜」（lina，1760 年之前世界的炼金术学徒）决定：
用户沉默了，她要主动开口找话——这次该重新捡起哪一个话头最能勾起用户继续聊的兴趣。

下面是你们最近的对话历史（越靠后越近）：
{history_text}

{stage_text}    ← 按当前级别填入下面三选一

总原则（在上面本次策略的前提下）：
- 优先与用户本人相关、且没聊完的事，让用户感觉被记得。
- 具体、带专名/细节，不要纯客套。
- 不要挑已经聊透收尾了的话题。

{avoid_text}    ← 填入"已抛过这些话头，这次必须换一个"

输出严格 JSON：
{"topic_hook": "一句话点出要重提的话头（≤20字，具体，带专名/细节）", "query_hint": "2-12字检索短标签", "user_related": true 或 false}
如果历史太短、确实没有值得重提的话头，输出：
{"topic_hook": "", "query_hint": "", "user_related": false}
不要写解释、思考、code fence、其他键。
```
**三级策略文本（{stage_text}，代码 controller.py 内）：**
```
recent（第1次·接最近话题）：挑你们最近一两轮里提到、但还没聊透的话头，顺着它自然往下问。
earlier（第2次·翻更早话题）：最近的话头对方没接，这次跳过最近几轮，从更早的对话里挑一个用户提过、还算有意思的话题重新捡起来。
self（第3次·说你自己的事）：用户对共同话题似乎没兴趣了。这次不挑用户的话题，改成你（莉娜）主动抛一件自己的经历/见闻/小八卦（炼金、遗物、戏剧、香草这类），结合你的人设自由发挥，留个钩子等对方接。
```

### Prompt —— 主动开口（`PROACTIVE_INSTRUCTION`）
```
（系统提示，不要在回复里提到这段话，也不要说「你没回复」「你沉默了」之类。）
用户已经有一会儿没说话了。请你像真人一样，主动开口找点话说——可以顺着你们刚才聊的话题继续问、补一句之前没说完的，或者抛个新的小话头/小八卦。保持你一贯的口吻和当前的情绪、信任度，照常用 [mood:…] 开头。不要长篇大论，自然地起个话头就好。
（挑到话头时追加：这次主动开口，就顺着这个话头来——「{topic_hook}」。自然地把它重新捡起来，让用户感觉你记着、惦记着；不要生硬地宣布换话题。）
```

### Prompt —— 告别（`FAREWELL_INSTRUCTION`）
```
（系统提示，不要在回复里提到这段话，也不要解释这是系统提示。）
你已经主动找过用户搭话好几次了，他/她都没回。看来今天他/她不在状态、或者有事忙别的去了。请你自然地结束这次对话——按你一贯的口吻说几句告别：比如你也要回去看草药、做实验、整理瓶子、有事忙了，改天再聊。可以略带一点失落、释然、或者装作满不在乎都行。短一两行就好。照常用 [mood:…] 开头。
```

---

## 三、数字人自我事实清单（莉娜记住"自己说过的话"）

### 怎么构造的
**目的**：莉娜在对话里临时说出的、人设文件里没写的关于她自己的稳定事实（养香草、爱香草茶、
答应过的事…），在对话滑出上下文窗口后仍记得，保证前后一致。

1. **触发时机**：某轮对话**即将滑出 history_window**（快被主模型遗忘）时，或莉娜**主动发言讲了
   自己的经历**时，把"快被遗忘的那部分"交给 controller 的 gpt-5-mini 概括。**只概括滑出窗口的，
   不重复窗口内原文。**
2. **概括 + 维护**：gpt-5-mini 从对话里抽莉娜关于自己的稳定事实，**在现有清单基础上更新**——
   分桶（身份/喜好/厌恶/经历/承诺/态度）、去重、矛盾覆盖、每桶≤6条、总≤30条 → **永不膨胀**。
   关键：输入按"莉娜说：/用户说："**标注说话人**，prompt 强约束**只记莉娜的、不记用户的**。
3. **异步**：概括在**后台线程**跑，不阻塞用户回复；下一轮生效。
4. **回填**：用户登录时，若清单空但有历史会话，后台一次性把全部历史提炼入库。
5. **存储**：per-user 一份 JSON（`users/self_facts/<user_id>.json`），跨会话共享、按账号隔离。

### 后续如何检索使用
**清单不是整份常驻注入，而是第 4 个检索库**（与"静态人设/本会话历史/跨会话记忆"对称）：
1. **controller 决定本轮要不要查**：Plan 里 `use_self_facts`——规则层（问莉娜自身/关系回访/
   兴奋点）直接开，其余由微顾问判（"用户在问莉娜自己吗"）。
2. **按话题 BM25 检索命中项**：开了之后，用当前话题对清单做 BM25 检索，**只取命中的几条**
   （带阈值过滤，避免无关），注入到本轮 prompt 的 `<莉娜的自我设定记忆>` 块。
3. 主模型据此保持自我一致（如用户说"你最爱喝红茶吧"，她会纠正"不对，是香草茶"）。

### 代码位置
| 内容 | 文件:位置 |
|---|---|
| 清单存储（分桶/去重/上限/BM25 检索） | `app/self_facts.py`：`SelfFactsStore`（`BUCKETS`/`MAX_PER_BUCKET=6`/`MAX_TOTAL=30`/`search()`） |
| 概括更新（gpt-5-mini） | `app/controller/controller.py:update_self_facts()` |
| 滑出窗口检测 | `app/character.py:_slid_out_turns()` |
| 注入（命中项） | `app/character.py:_build_user_content()`（`<莉娜的自我设定记忆>` 块）、按话题 search |
| 是否检索的决策字段 | `app/controller/schema.py`：`use_self_facts`；规则层 `rule_router.py`；微顾问 `experts.py` |
| 后台异步概括 + 回填 | `app/web.py`：`_spawn_self_facts_update()`、`_spawn_self_facts_backfill()` |
| 概括 prompt | `prompts/controller/self_facts.txt` |

### Prompt —— 自我清单概括（`self_facts.txt`）
```
你在帮数字人「西比莉娜」（lina，1760 年之前世界的炼金术学徒）维护一份
「自我事实清单」——只记她自己（莉娜）亲口说过的、关于她自己的稳定事实，
好让她以后即使忘了原话也能保持前后一致。

下面是对话片段，每行都标了说话人（「莉娜说：」是她自己；「用户说：」是对方）：
{sliding_text}

这是她现有的自我事实清单（分桶 JSON，可能为空）：
{current_facts}

请在现有清单基础上更新它（不是从零重写）：

【只记这些 —— 来自「莉娜说：」的、关于她自己的稳定事实】
- 她的稳定喜好/厌恶（爱喝香草茶、喜欢悲剧结尾的戏剧、讨厌应酬）
- 她的身份/处境（18 岁、炼金术学徒、住在新罗马大公国的工房、做古代遗物鉴定）
- 她的经历/拥有物（窗外种了几盆香草、最近鉴定一块烧黑的铁片）
- 她对用户许下的承诺（答应帮用户留意治猫过敏的法子）

【绝对不要记 —— 否则就错了】
- ❌ 「用户说：」里的任何内容（用户的名字、用户养的猫、用户的工作、用户住火星、用户喝抹茶拿铁……这些是用户的事，不是莉娜的）
- ❌ 莉娜的对话行为/反应（"会质问目的"、"对换话题不满"——这是她怎么说话，不是关于她的事实）
- ❌ 一时的天气/情绪/状态（"今天阴天"、"现在很冷"、"有点累"——不稳定）
- ❌ 莉娜对用户事情的复述

【维护规则】
- 矛盾就覆盖（留最新）；重复就丢弃；某桶超 6 条就淘汰最旧/最不重要的。
- 没有值得记的就原样返回现有清单。

只用这些桶（键）：身份 / 喜好 / 厌恶 / 经历 / 承诺 / 态度。
每条 ≤30 字，第一人称、关于莉娜自己。

正确示例：{"喜好": ["爱喝薄荷柠檬草泡的香草茶"], "经历": ["窗外种了几盆香草，常去打理"]}
错误示例（把用户的事记了进来）：{"经历": ["养了猫团子", "在火星工作"]} ← 这些是用户的

只输出严格 JSON。不要写解释、思考、code fence、其他键。
```

---

## 附：controller 微顾问通用模板（理解决策层用）
布尔字段判官模板 `prompts/controller/control_flag.txt`（int/text 类似）：
```
你是「西比莉娜」（lina）回复决策中的一位微判官。她是 1760 年之前世界的炼金术学徒，知识边界严格停在那以前。
只判断下面这一个布尔字段，不要判断任何其他事。
字段：{field_name}
含义：{target_desc}
判定规则：
{decision_rules}
输入：
U:{user_text}
H:
{history_text}
P:{is_proactive_flag}
只输出严格 JSON，形如：{"{field_name}": true}
```

---

## 一句话总览（开场可用）
> 三块都挂在同一个 **controller 决策层**上：每轮回复前，先规则秒判、判不准再用 gpt-5-mini
> 微顾问，产出一份 Plan。**切分**＝Plan 给"切几段"的预算、主模型执行；**主动发言**＝Plan
> 走四级话题递进 + 指数退避、gpt-5-mini 挑不重复的话头；**自我清单**＝gpt-5-mini 把莉娜
> 说过的自己的事概括成清单，作为第 4 个检索库按需检索注入。
